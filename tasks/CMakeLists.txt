# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(oneway_testTaskLib)
ADD_LIBRARY(${ONEWAY_TEST_TASKLIB_NAME} SHARED 
    ${ONEWAY_TEST_TASKLIB_SOURCES})
add_dependencies(${ONEWAY_TEST_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${ONEWAY_TEST_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ONEWAY_TEST_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ONEWAY_TEST_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ONEWAY_TEST_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${ONEWAY_TEST_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${ONEWAY_TEST_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ONEWAY_TEST_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ONEWAY_TEST_TASKLIB_HEADERS}
    DESTINATION include/orocos/oneway_test)

